
### Обновляем

    sudo apt-get update

    sudo apt-get upgrade

### Проги 

    sudo apt-get intsall vim

### Установка .deb файла

    sudo dpkg -i *.deb
    
    sudo apt install curl wget git
# terminals

    sudo apt-get install tmux
